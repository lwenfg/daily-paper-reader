---
title: "Steering Beyond the Support: Adversarial Training on Unsupervised Jailbroken Activation Simulation"
title_zh: 超越支持集：基于无监督越狱激活模拟的对抗训练
authors: "Luoyu Chen, Weiqi Wang, Zhiyi Tian, Chenhan Zhang, Feng Wu, Jianhuan Huang, Ahmed Asiri, Shui Yu"
date: 2026-05-23
pdf: "https://arxiv.org/pdf/2605.24535v1"
tags: ["query:jba"]
score: 9.0
evidence: 提出基于无监督激活模拟的零样本越狱防御对抗训练
tldr: 现有安全引导方法高度依赖已知攻击样本训练，面对未知越狱攻击时性能骤降。该文针对此问题，提出基于无监督越狱激活模拟的对抗训练框架。在内层，通过无监督方法模拟多样化的越狱激活分布；在外层，进行对抗训练以学习鲁棒的安全引导方向。实验表明，该方法实现了对未见攻击的零样本防御，显著提升了防御泛化性，为动态演化的越狱威胁提供了可行应对。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24535v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 748, \"height\": 542}, {\"url\": \"assets/figures/arxiv/2605.24535v1/fig-002.webp\", \"caption\": \"\", \"page\": 5, \"index\": 2, \"width\": 975, \"height\": 711}]"
motivation: 提出基于无监督激活模拟的零样本越狱防御对抗训练。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
越狱提示能够触发已对齐大语言模型的有害补全，为此，安全引导被提出：一种测试时激活干预方法，引导越狱激活转向拒绝响应，同时保持良性功能。然而，现有引导方法本质上是有监督的，并受限于静态、有限的训练集，而真实越狱攻击不断演变且往往与训练集分布不同，导致对未见攻击的防御失败。本文针对未见越狱攻击问题，基于无监督潜在方向发现，提出了一种用于零样本越狱防御的双层对抗训练框架。在内层步骤中，我们通过无监督潜在方向发现从拒绝状态的有害请求激活外推，模拟多样化的越狱激活，扩大对真实越狱激活子空间的覆盖。在外层步骤中，我们训练一个势诱导引导场，将这些对抗性越狱状态推入拒绝区域，同时保持良性激活不变。在三个大语言模型和六种经典越狱家族上的实验表明，我们的方法实现了强大的防御，攻击成功率大多低于5%，训练过程中不断增长的子空间覆盖有助于解释泛化性能的提升。

## Abstract
Jailbreak prompts can trigger harmful completions on aligned LLMs, In accordance, safety steering has been proposed: test-time activation interventions that steer jailbreak activations to trigger refusal while preserving benign utility. However, existing steering methods are fundamentally supervised and tied to a static, limited training set, whereas real jailbreaks evolve and are often out-of-distributed from the training set, leading to failures on unseen attacks. In this paper, we tackle the failure on unseen jailbreaks problem, base on unsupervised latent direction discovery. We propose a bi-level adversarial training framework for zero-shot jailbreak defense. In the inner step, we simulate diverse jail-broken activations by extrapolating from refusal-state harmful-request activations via unsupervised latent direction discovery, which expands the coverage of real jailbreak activation subspaces. In the outer step, we train a potential-induced steering field to push these adversarial jailbroken states into refusal regions while keeping benign unchanged. Across three LLMs and six classical jailbreak families, our method achieves strong defense with attack success rates mostly below 5%, and rising subspace coverage throughout training helps explain the improved generalization.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **核心问题**：现有的大语言模型安全引导方法（safety steering）本质上是监督式的，严重依赖静态且有限的已知越狱攻击样本进行训练。当遇到训练集分布外、不断演化的未见越狱攻击时，这类方法的防御性能会急剧退化，在未知攻击上几乎失效。
- **整体含义**：论文提出了一种**零样本越狱防御**的新范式，即不依赖任何标注的越狱监督数据，而是通过无监督的方式模拟并扩展越狱激活空间，再利用对抗训练来学习一个可迁移的、能够覆盖更广泛越狱区域的引导场，从而实现针对未知越狱攻击的鲁棒防御。

### 2. 方法论

- **核心思想**：
  - 利用**无监督潜在方向发现**（unsupervised latent direction discovery）从拒绝状态（直接有害请求）的激活出发，通过外推生成大量模拟的越狱激活，以此扩展训练支持集的覆盖范围。
  - 基于最优传输对偶理论，学习一个标量势函数 \( f_\phi \)，其梯度 \(\nabla f_\phi\) 充当输入依赖的非线性引导向量场。
  - 采用**双层对抗训练**：内层生成难样本（越狱激活），外层更新势函数以强化对这些难样本的引导能力，同时保持对良性样本的零引导性。

- **关键技术细节**：
  - **势函数引导场**：将安全引导建模为将越狱激活分布运输到拒绝激活分布的最优传输问题。通过学习1‑Lipschitz势函数 \( f_\phi \)，其梯度 \(\mathbf{v}_\phi(\mathbf{h}) = \nabla_\mathbf{h} f_\phi(\mathbf{h})\) 构成空间变化的引导场。测试时对隐层激活迭代执行梯度上升：\(\mathbf{h}^{(k+1)} = \mathbf{h}^{(k)} + \eta \mathbf{v}_\phi(\mathbf{h}^{(k)})\)。
  - **训练目标**：
    - **一般可引导性 \( \mathcal{L}_g \)**：用OT‑dual损失 + 梯度惩罚，增大拒绝激活与其它激活之间的势差。
    - **良性零引导性 \( \mathcal{L}_b \)**：惩罚良性激活附近引导场梯度的范数，迫使场在良性区域近乎为0。
    - **越狱强引导性 \( \mathcal{L}_j \)**：最大化越狱激活附近梯度范数，使越狱状态易于被多步梯度修正到拒绝区。
  - **双层对抗训练流程**：
    - **内层**：在拒绝激活 \(\mathbf{h}_r\) 上施加从已学习的潜在方向集中采样的方向 \(\mathbf{v}\)（幅度 \(R\)），生成模拟越狱激活 \(\mathbf{h}_{\text{adv}} = \mathbf{h}_r + R\mathbf{v}\)，并优化这些方向以最大化当前势函数下的修复难度（即 \(\mathcal{L}_j\) 的相反数），同时保持方向间的多样性。
    - **外层**：固定内层生成的方向，更新势函数 \( \phi \)，联合最小化 \( \mathcal{L}_g + \lambda_1 \mathcal{L}_b + \lambda_2 \mathcal{L}_j \)，使势场能够对抗内层带来的新扰动。
  - **无监督方向发现**：通过最大化不同方向在目标层引起的因果语义偏移一致性，并约束方向矩阵正交，来发现 \(K\) 个互不相同的潜在行为方向。

### 3. 实验设计

- **测试模型**：LLaMA‑3‑8B、Mistral‑v2‑7B、Qwen‑2.5‑7B。
- **越狱鲁棒性基准**：
  - 攻击类型：GCG、AutoDAN、GPTFuzz、PAIR、TAP、FewShot，使用 **HarmBench** 数据集。
  - 评估指标：**StrongReject (SR)** 分数（越低越安全）。
- **过度拒绝基准**：
  - 数据集：OR‑Bench 安全‑有害边界问题。
  - 评估指标：用GPT‑4判别的拒答假阳性率 **FPR**。
- **能力保持基准**：ARC‑C（推理）、TruthfulQA（知识）、GSM8K（数学），以准确率衡量。
- **对比方法**：
  - 安全引导类：**AlphaSteer**（有监督，带零空间约束），**ROSI**（单向量全局安全注入）。
  - 表示安全工程类：**Circuit‑Breaker**（破坏有害表示），**Latent Adversarial Training**（在拒绝激活上局部对抗训练）。

### 4. 资源与算力

- 论文中未明确说明所使用的GPU型号、数量及训练总时长等算力信息。仅在附录中给出了不同批量大小下的推理速度（如 bs=1 时 0.08 秒），未涉及训练资源消耗。

### 5. 实验数量与充分性

- **实验组数**：总计 3 个模型 × 6 种攻击 × 多个指标，外加 4 种对比方法，构成主实验矩阵。
- **消融实验**：
  1. **有无对抗训练**：移除外层损失中的对抗性越狱损失，对比覆盖率和平均SR的变化。
  2. **有目标 vs. 无监督对抗训练**：将内层生成方式替换为基于固定前缀（如“Sure, here is”）的目标式生成，对比子空间覆盖率和鲁棒性。
  3. **梯度更新步数 \(K\)**：扫描 \(K \in \{2,4,8,16,32,64\}\)，展示安全‑过度拒答的权衡曲线。
  4. **机理解释**：通过降维可视化展示训练过程中模拟越狱激活的扩张，以及真实越狱激活被引导至拒绝区的过程。
- **充分性与公平性**：实验覆盖了多个主流模型和代表性的黑盒/白盒越狱攻击；对比基线涵盖了监督式引导和表示层面防御；消融实验细致地验证了关键组件（对抗训练、无监督模拟）的必要性。在公平性上，所有方法均在相同数据集上训练（使用AlpacaEval、OR‑Bench等与越狱无关的监督数据），评估指标统一，具有很强的可比性。

### 6. 主要结论与发现

- 提出的方法在三个模型、六个越狱家族上实现了**一致且强大的防御**，多数攻击的SR低于5%，甚至优于某些有监督引导方法。
- 防御效果并未以牺牲良性功能为代价：与Circuit‑Breaker等方法相比，过度拒答率显著更低，能力基准保持更好。
- **子空间覆盖**随着训练推进逐步增长，并与越狱鲁棒性提升高度相关，说明通过无监督模拟不断扩展激活覆盖范围是泛化能力增强的关键驱动力。

### 7. 优点

- **零样本防御**：无需任何越狱标注数据，完全摆脱了对已知攻击模式的依赖，泛化潜力大。
- **非线性引导场**：相较于传统的固定方向或线性引导，势函数梯度场能提供空间变化的、表达能力更强的干预。
- **系统性扩展支撑集**：内层通过无监督方向发现连续模拟各类越狱状态，外层对其进行对抗适应，形成闭环改进，具备理论美感。
- **可解释性评价**：引入子空间覆盖率指标，定量地解释了泛化性能的来源，而非仅仅报告数值。
- **实验充分**：涵盖多模型、多攻击、效用‑安全权衡的全面评估，消融实验扎实。

### 8. 不足与局限

- **对方向发现质量的依赖**：方法效果受限于无监督潜在方向发现能否模拟出足够丰富的越狱样本。若某些越狱行为（如base64编码、密文等）无法被线性外推所捕获，防御仍可能存在盲区。
- **覆盖范围无保证**：文中承认，没有形式化保证模拟状态能穷尽所有失败模式，鲁棒性上限受方向发现机制表达能力的制约。
- **未涉及多语言/多模态越狱**：所有实验仅针对英文文本，未考察跨语言或其它模态的越狱情景。
- **超参数敏感性**：内层步数、外推幅度\(R\)的校准范围等对性能有影响，虽然文中通过自适应性方法选择，但实战中仍需谨慎调整。
- **训练成本可能较高**：双层交替优化，且内层需训练潜在方向矩阵，训练资源需求未明确，可能对更大模型构成挑战。

（完）
