---
title: "BaxBench: Can LLMs Generate Correct and Secure Backends?"
title_zh: BaxBench：大语言模型能生成正确且安全的后端代码吗？
authors: "Mark Vero, Niels Mündler, Victor Chibotaru, Veselin Raychev, Maximilian Baader, Nikola Jovanović, Jingxuan He, Martin Vechev"
date: 2025-05-01
pdf: "https://openreview.net/pdf?id=il3KRr4H9u"
tags: ["query:safe-coding"]
score: 9.0
evidence: 评估大语言模型生成安全正确后端代码的基准测试
tldr: 现有基准多评估LLM函数级代码生成，本工作提出BaxBench，包含392个后端应用生成任务，专注于评估LLM生成正确且安全的后端代码的能力。实验表明当前LLM难以可靠满足安全需求，凸显了安全代码生成的挑战。该基准为安全AI代码生成提供了全面的评测平台。
source: ICML-2025-Accepted
selection_source: conference_retrieval
motivation: 现有基准缺乏对LLM生成安全后端应用的系统评估。
method: 构建包含392个后端任务的BaxBench，重点评估代码安全与正确性。
result: 实验显示当前LLM在满足后端安全要求方面表现不佳。
conclusion: BaxBench为安全AI代码生成提供了关键评测基准和方向指引。
---

## Abstract
Automatic program generation has long been a fundamental challenge in computer science. Recent benchmarks have shown that large language models (LLMs) can effectively generate code at the function level, make code edits, and solve algorithmic coding tasks. However, to achieve full automation, LLMs should be able to generate production-quality, self-contained application modules. To evaluate the capabilities of LLMs in solving this challenge, we introduce BaxBench, a novel evaluation benchmark consisting of 392 tasks for the generation of backend applications. We focus on backends for three critical reasons: (i) they are practically relevant, building the core components of most modern web and cloud software, (ii) they are difficult to get right, requiring multiple functions and files to achieve the desired functionality, and (iii) they are security-critical, as they are exposed to untrusted third-parties, making secure solutions that prevent deployment-time attacks an imperative. BaxBench validates the functionality of the generated applications with comprehensive test cases, and assesses their security exposure by executing end-to-end exploits. Our experiments reveal key limitations of current LLMs in both functionality and security: (i) even the best model, OpenAI o1, achieves a mere 62% on code correctness; (ii) on average, we could successfully execute security exploits on around half of the correct programs generated by each LLM; and (iii) in less popular backend frameworks, models further struggle to generate correct and secure applications. Progress on BaxBench signifies important steps towards autonomous and secure software development with LLMs.

---

## 论文详细总结（自动生成）

# 论文总结：BaxBench: Can LLMs Generate Correct and Secure Backends?

## 1. 论文的核心问题与整体含义
* **核心问题**：当前大语言模型（LLM）在代码生成上的评估大多停留在函数级别、代码编辑或算法题，这些任务远不足以体现生产环境的复杂需求。真正需要全自动化生成的，是**功能完整、安全可靠的后端应用模块**。
* **整体含义**：论文旨在回答“LLM 能不能生成正确且安全的后端代码”这一关键问题，并推动自动安全软件开发的研究。为此，作者提出了专门的评估基准 BaxBench，重点考察**功能正确性**与**抗攻击安全性**，揭示现有模型的严峻局限性。

## 2. 论文提出的方法论
* **核心思想**：构建一个面向后端应用生成的双维度评估平台，同时测量生成代码的功能完备性和安全脆弱性，迫使模型不仅要“写对代码”，还要“写得安全”。
* **技术细节**：
  * **任务构建**：从真实的 Web/云软件场景中提取了 392 个后端生成任务，每个任务要求生成包含多个文件、多个函数的完整应用模块。
  * **功能验证**：为每个任务准备了**全面的测试用例**，通过执行测试来判定生成的后端是否实现了所需功能。
  * **安全评估**：采用**端到端的漏洞利用执行**：对生成的正确程序直接运行真实的攻击脚本，若攻击成功则说明代码存在安全隐患。这种评估比静态安全扫描更贴近实际。
  * **框架多样性**：任务覆盖不同流行程度的后端框架，以便分析模型对少见框架的泛化能力。
* **算法流程**：基本流程为：给定任务描述 → LLM 生成多文件后端代码 → 执行功能测试 → 若通过测试，则执行一组预定义的安全漏洞利用 → 记录正确性与安全性结果。

## 3. 实验设计
* **数据集/场景**：BaxBench 自建的 392 个后端应用生成任务，覆盖主流与部分较冷门的后端框架（具体名称未在摘要中展开）。
* **评估基准**：自身的 BaxBench 基准，包含功能测试用例和安全漏洞利用集。
* **对比方法**：对比了当前多个前沿的 LLM，明确提及 **OpenAI o1** 作为表现最好的模型，暗示也评估了其他模型（如 GPT-4 系列、开源模型等，摘要未逐一列出）。直接在同一个基准上公平比较不同模型的生成质量。

## 4. 资源与算力
* **文中说明**：提供的摘要和元数据中**未明确提及**具体的算力配置，如 GPU 型号、数量、训练时长等。由于该工作主要是推理评估，并非模型训练，所需算力可能主要是 API 调用和本地执行测试环境，未刻意突出资源消耗。

## 5. 实验数量与充分性
* **大致实验数量**：摘要没有给出详细的实验组数，但从描述可推断至少包含了：多个模型 × 392 个任务 × (功能 + 安全) 两种评估维度的完整测试。很可能还进行了**不同框架类别**的分析，甚至消融实验未提及。
* **充分性与公平性**：
  * **充分性**：392 个后端任务规模较大，且要求多文件、多功能，贴近实际；同时用端到端漏洞利用评估安全性，比只测功能更严苛，实验设计全面。
  * **公平性**：所有模型面对相同的任务、相同的测试用例和相同的攻击方法，结果可直接比较，具备客观公平性。

## 6. 论文的主要结论与发现
* **功能正确性严重不足**：即便是最强的模型（OpenAI o1），在 BaxBench 上的代码正确率也仅有 **62%**，尚无法可靠地生成生产级后端代码。
* **安全防护普遍薄弱**：在各模型生成的**正确程序中，平均约有一半**能够被成功实施安全漏洞利用，表明模型难以同时满足功能与安全需求，安全性是重大短板。
* **框架流行度影响显著**：对于**流行度较低的后端框架**，模型生成正确且安全代码的能力进一步下降，反映出对训练数据的分布依赖。
* **整体含义**：BaxBench 上的进展将标志着向 **“使用 LLM 进行自主且安全的软件开发”** 迈出重要一步，但目前距离这个目标还很遥远。

## 7. 优点：方法或实验设计上的亮点
* **双维度评估**：首次将**功能正确性和运行时安全性**紧密结合在一个基准中，弥补了现有基准只重视功能而忽略安全的空白。
* **实战化安全测试**：使用真实的端到端漏洞利用而非静态规则扫描，更准确地反映生成的代码在部署后可能面临的风险。
* **后端聚焦**：后端应用是云服务的核心，且多文件、多交互的设计难度远高于单函数，使得评估结果对产业界有直接参考价值。
* **任务规模与多样性**：392 个任务并涵盖不同框架，增加了基准的挑战性和泛化分析能力。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
* **领域局限**：只覆盖了后端应用，未涉及前端、全栈或系统级编程等安全关键领域，结论不能简单外推。
* **语言与框架覆盖**：摘要未列出具体编程语言和框架，可能集中于某几种流行语言（如 Python、Node.js），对其他语言生态的代表性存疑。
* **安全评估的静态性**：尽管漏洞利用是动态执行的，但利用脚本是固定的，可能无法捕捉所有类型的安全问题（例如业务逻辑漏洞）。
* **模型覆盖**：虽然提到多种 LLM，但摘要只点名了 OpenAI o1，缺少全面对比的详细分析；也未涉及经过安全微调的模型变体。
* **修复与迭代能力未评估**：基准只测“一次生成”的质量，未考察模型根据安全反馈进行代码修复的能力，而这也是实际开发的重要环节。
* **潜在偏差**：任务的构造方式可能无意中偏向某些代码模式，影响评估的普适性，但论文正文本文中如何缓解未在摘要中说明。

（完）
