---
title: "Fun2spec: Code Contract Synthesis At Scale"
title_zh: Fun2spec：大规模代码契约综合
authors: "Shubham Ugare, Tarun Suresh, Sasa Misailovic, Julien Vanegue"
date: 2025-09-19
pdf: "https://openreview.net/pdf?id=Z7TUUQPSzY"
tags: ["query:safe-codegen"]
score: 7.0
evidence: 综合代码契约以验证代码安全
tldr: 针对代码规范综合不可判定、手工编写成本高的问题，Fun2spec利用大语言模型自动生成代码契约候选，并通过基于测试的预言验证其有效性。该方法首次在真实大规模C代码库上实现了可扩展的契约综合，生成的契约至少与已有测试套件同等准确。这一工具可集成到安全代码生成流程中，用于验证自动生成代码的安全性，显著降低了形式化验证的门槛。
source: ICLR-2026-Public
selection_source: conference_retrieval
motivation: 代码规范手工编写困难，但为保障软件安全至关重要。
method: 使用LLM生成候选代码契约，通过测试预言筛选验证有效的契约。
result: 在真实大规模C代码中成功综合出经过验证的代码契约。
conclusion: 为自动化代码安全验证和规范生成提供了可扩展的解决方案。
---

## Abstract
Specification synthesis -- the problem of inferring program specification from program implementation -- is an undecidable problem. Therefore, machine learning and more specifically, autoregressive large language models (LLMs) are a good fit for solving this problem. We demonstrate that code contracts, the most widespread software specification mechanism, can be generated by LLMs and validated using a software testing oracle on large real-world codebases. This oracle acts as an approximate code contract verifier. It only selects LLM-generated contracts that are test-valid, guaranteeing that the obtained contracts are at least as good as the test suite provided by the program. Our tool Fun2spec is the first code contract synthesis tool applied to real-world large-scale low-level C and C++ programs of millions of lines of code, raising the state-of-the-art of automated software verification using LLMs.

---

## 论文详细总结（自动生成）

# 论文总结：Fun2spec – 大规模代码契约综合

## 1. 核心问题与整体含义
- **背景问题**  
  代码规范的自动综合是一个不可判定问题，因此传统方法难以实现高精度、可扩展的自动化。
- **手工困境**  
  代码契约（如前置/后置条件、不变量）是保障软件安全的关键，但人工编写耗时、易错，尤其在大规模 C/C++ 代码库中成本极高。
- **整体含义**  
  Fun2spec 首次在大规模真实 C/C++ 代码（数百万行）上实现自动代码契约的综合，利用大语言模型（LLM）生成候选契约，再通过测试预言进行筛选，显著降低了形式化验证的门槛，为“安全代码生成”流程提供了自动化验证手段。

## 2. 方法论
- **核心思想**  
  将规范综合问题视为“生成+验证”两步流水线：LLM 提供“假设性”契约，测试执行充当“近似验证器”筛选出真正符合现有测试套件的契约。
- **关键技术细节**
  - **LLM 生成阶段**  
    使用自回归大语言模型基于函数实现生成契约候选（如前置条件、后置条件、断言等）。
  - **测试预言验证**  
    在程序现有的测试套件上动态检查契约是否成立，只保留那些在所有测试中未被违反的契约（即“测试有效”的契约）。
  - **保证性**  
    由于依赖程序自带的测试套件，获得的契约至少和测试套件一样准确，不会引入与测试行为不一致的错误规范。
- **算法流程（文字描述）**
  1. 输入：程序源码 + 已有测试套件。
  2. 对每个待分析函数，利用 LLM 生成 K 个候选契约。
  3. 插入契约到源代码中，编译并运行测试。
  4. 评估每个契约在测试中的通过率，只保留完全通过测试的契约。
  5. 输出“测试有效”的代码契约集合。

## 3. 实验设计
- **数据集/场景**  
  在真实的大规模 C 和 C++ 开源项目上进行评估，代码规模达到数百万行级别。
- **Benchmark**  
  以程序自带的测试套件作为 ground‑truth 的替代品（即测试有效契约作为成功指标）。
- **对比方法**  
  文中未详细列出对比对象，但从摘要可推断与手工编写规范、传统规范推断工具的对比（首次在如此大规模低级语言上做到自动化）。
- **额外评估**  
  可能包含契约准确性、覆盖率、与已有测试一致性等指标的分析。

## 4. 资源与算力
- **文中信息缺乏**  
  所提供的元数据及摘要中并未说明使用了何种 GPU（型号、数量）、训练时长或推理成本。
- **推断**  
  Fun2spec 可能主要依赖第三方 LLM（如 GPT 系列）的 API 调用，或使用开源模型进行推理，但具体算力配置未知。

## 5. 实验数量与充分性
- **实验组数推测**  
  基于元数据，至少包含在不同项目（多代码库）上的整体效能实验，可能还包含消融实验（如无测试过滤的 LLM-only 生成 vs. 有测试验证的生成）、不同生成候选数 K 的影响等。
- **充分性与客观性**
  - **充分性**：在数百万行代码上成功运行，说明扩展性良好。
  - **公平性**：使用项目自身测试套件作为筛选标准，避免了外部标准的主观性，但可能受测试套件质量影响。
  - **潜在局限**：未与其他契约综合工具的全面对比（若未做），或缺少人工评估标杆。

## 6. 主要结论与发现
- LLM 能生成代码契约，但直接生成的契约可能不准确。
- 通过测试预言过滤后，得到的契约至少与现有测试套件一样准确，有效提升了自动化软件验证的水平。
- Fun2spec 是首个在真实大规模 C/C++ 代码库上实现契约综合的工具，证明了该方法的可扩展性和实用性。
- 该技术可集成到安全代码生成管道中，作为生成代码正确性的自动检查器。

## 7. 优点
- **首次解决大规模问题**  
  将契约综合从研究原型推向了百万行工业代码，填补了自动化验证在低级语言上的空白。
- **实用的验证策略**  
  以现有测试套件为“预言”具有工程友好性，不需要额外形式化规范。
- **即插即用潜力**  
  可作为代码生成后静默验证的安全网，提升 AI 代码生成的可信度。
- **输出可靠**  
  测试验证机制筛除了与项目已知行为矛盾的契约，保证了实用性。

## 8. 不足与局限
- **依赖测试套件质量**  
  如果测试覆盖不全或薄弱，生成的契约可能过于宽松或遗漏关键属性。
- **契约深度未知**  
  可能仅生成浅层的不变量或属性，难以捕捉复杂深层语义。
- **LLM 偏差风险**  
  训练数据中的代码风格和规范习惯可能引入系统偏见。
- **计算成本未说明**  
  若需多次调用 LLM 或大规模测试，成本可能较高，但论文未提供量化数据。
- **通用性与公平性**  
  未提及其他类型规范（如时序逻辑）的验证，也未与最新规范综合工具（如基于抽象解释的工具）进行细粒度对比。

（完）
